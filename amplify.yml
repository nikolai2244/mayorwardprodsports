version: 1

frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Build complete"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
